# CamaraSim — Network Access Domains, API version vwip
# (CAMARA NetworkAccessManagement / Network Access Domains, wip).
#
# Vendored from the CAMARA NetworkAccessManagement "Network Access Domains" API
# (work-in-progress — no released version yet, so mounted at its canonical `vwip`
# base path `/network-access-domains/vwip`, like its sibling Network Access
# Devices and the other pre-1.0 wip APIs) and annotated with CamaraSim's
# functional cases (docs/DESIGN.md §7, §9). Backed by
# `src/apis/network_access_domains/vwip.rs`; spec and code must not drift.
#
# So far this vendored slice serves two read-only endpoints:
#   * `GET /trust-domains/capabilities` (getTrustDomainCapabilities) — the
#     provider-level capabilities document describing which Trust Domain access
#     types and policy limits this API provider supports. It has no request body
#     and no device identifier, so — unlike the identifier-keyed APIs — it carries
#     no reserved-error scenario plane: it always answers the same fixed document.
#   * `GET /services` (getServices) — the caller's Services catalog. It has no
#     request body either, but it is keyed to the authenticated identity, so its
#     control plane is the token subject (DESIGN §7): a reserved error suffix →
#     the canonical CAMARA error; else the subject's trailing three digits fix the
#     returned ServiceList deterministically.
# It also serves the single-service `GET /services/{serviceId}` read
# (getService), the stateful Trust Domain create leg
# `POST /trust-domains` (createTrustDomain), its read-back
# `GET /trust-domains/{trustDomainId}` (getTrustDomain), its in-place update leg
# `PATCH /trust-domains/{trustDomainId}` (updateTrustDomain), and its delete leg
# `DELETE /trust-domains/{trustDomainId}` (deleteTrustDomain), backed by the
# in-memory trust-domain store. The Trust Domain Device CRUD resource remains to
# be added in later slices.
openapi: 3.0.3
info:
  title: Network Access Domains
  description: |
    Service for configuring **home-network access domains** — Trust Domains that
    group devices under a common access policy (Wi-Fi or Thread) on the
    subscriber's home network. This is part of the CAMARA Home Devices family and
    the CamaraSim reference double for the CAMARA NetworkAccessManagement
    "Network Access Domains" API (wip).

    ## `GET /trust-domains/capabilities`

    Returns the set of Trust Domain configuration capabilities the API provider
    supports: which access types (Wi-Fi WPA-Personal/Enterprise, Thread) and
    their properties, plus the policy limits (max devices, per-domain
    up/downstream bandwidth bands, egress-list constraints) a caller may configure
    when creating a Trust Domain. It requires the `network-access-domains:trust-domains`
    scope.

    ## `GET /services`

    Returns the caller's **Services** catalog — a `ServiceList` of the logical
    commercial subscriptions / account relationships the authenticated identity
    holds, each tied to a `serviceSite` (a subscriber location). It requires the
    `network-access-domains:services:read` scope.

    ## `GET /services/{serviceId}`

    Returns a single **Service** from that catalog by id. It requires the same
    `network-access-domains:services:read` scope.

    ## `POST /trust-domains`

    Creates a **Trust Domain** for one of the caller's services (a `serviceId`, a
    `name`, an `enabled` flag, and 1-4 `accessDetails`), mints a server-assigned
    `trustDomainId`, and returns the full `TrustDomain` (`201`). It requires the
    `network-access-domains:trust-domains` scope. This is CamaraSim's first
    stateful Trust Domain leg — the created Trust Domain is held in an in-memory
    store so later read/update/delete legs can address it.

    ## `GET /trust-domains/{trustDomainId}`

    Reads a **Trust Domain** back by the server-assigned `trustDomainId` that
    `createTrustDomain` returned. It requires the same
    `network-access-domains:trust-domains` scope. The id is opaque
    (SHA-256-derived), so the in-memory store is the only control plane: a stored
    id → `200` with the persisted `TrustDomain`, any other id → `404 NOT_FOUND`.

    ## Functional cases — the input is the control plane (docs/DESIGN.md §7)

    **`getTrustDomainCapabilities`** — provider capabilities are fixed operator
    configuration, not keyed to any device or subscriber, so this operation has
    **no** parameter-driven control plane: given a token carrying the required
    scope it always answers the same deterministic `TrustDomainCapabilities`
    document (`200`).

    **`getServices`** has no request body either, but it is keyed to the
    authenticated identity, so its control plane is the **token subject**: a
    reserved error suffix on the subject → the canonical CAMARA error; otherwise
    the subject's trailing three digits `d` fix the catalog deterministically —
    `d == 0` (`…000` / no digits) → `200 []` (nothing associated; a list never
    404s), else `((d - 1) % 3) + 1` services (1–3), each with a deterministic
    UUID-shaped `id` and `serviceSite`.

    **`getService`** (`GET /services/{serviceId}`) reads one `Service` from that
    same identity-keyed catalog. It regenerates the subject's catalog (no store)
    and matches the `serviceId` path parameter. Two control planes (DESIGN §7):
    the subject's reserved error suffix → the canonical CAMARA error (an
    account-level plane, checked first, mirroring `getServices`); otherwise the
    `serviceId` vs the catalog — an id the identity holds → `200` that `Service`,
    any other id (unknown / another identity's / malformed) → `404 NOT_FOUND`.
    The `serviceId` is opaque to the caller (SHA-256-derived), so it is not itself
    a reserved-error plane; because the catalog is regenerated rather than stored,
    a malformed id is indistinguishable from an unknown one and folds into the same
    `404` (mirroring the sibling `getNetworkAccessDevice`).

    For all three, a missing/invalid token → `401`; a token without the
    endpoint's scope → `403` (both from the shared resource-server layer).

    **`createTrustDomain`** (`POST /trust-domains`) creates a Trust Domain from a
    `TrustDomainCreate` body. Three control planes (docs/DESIGN.md §7): the token
    subject's reserved error suffix → the canonical CAMARA error (an account-level
    plane, checked first); request validation → `400 INVALID_ARGUMENT`; and the
    in-memory store — the `trustDomainId` is derived from the `(serviceId, name)`
    pair, so re-creating the same name for the same service collides → `409`.

    **`getTrustDomain`** (`GET /trust-domains/{trustDomainId}`) reads a created
    Trust Domain back. The `trustDomainId` is an opaque, server-minted UUID, so —
    mirroring the sibling `readNetwork` / `getApp` read legs — the in-memory store
    is the only control plane: a stored id → `200` with the persisted
    `TrustDomain`, any other id (never created or malformed) → `404 NOT_FOUND`
    (a malformed id folds into the same `404`, no store entry to tell them apart).

    **`updateTrustDomain`** (`PATCH /trust-domains/{trustDomainId}`) updates a
    created Trust Domain in place from a `TrustDomainUpdate` (every field optional).
    Two control planes: the request body (validated first, so a body `400` wins over
    a `404`) and store state (a stored id → `200` with the updated `TrustDomain`,
    any other id → `404`). A present field replaces, an explicit `null` on a
    clearable optional (`description`/`expiration`/`policies`) removes it, and
    `accessDetails` replaces wholesale; the read-only identity/audit fields are
    immutable and `modifiedAt`/`modifiedBy` are re-stamped. There is no `409` — the
    resource is addressed by its fixed id, so a rename can't collide.

    **`deleteTrustDomain`** (`DELETE /trust-domains/{trustDomainId}`) evicts a
    created Trust Domain. Keyed only to the store, mirroring `getTrustDomain`: a
    stored id is removed → `204 No Content` (single-use — a second delete of the
    same id finds nothing), any other id (never created, already deleted, or
    malformed) → `404 NOT_FOUND`. Deletion is synchronous with no CloudEvent.

    ## Documented cuts

    - The read-only `getTrustDomainCapabilities`, `getServices` and `getService`
      legs and the stateful `createTrustDomain` / `getTrustDomain` /
      `updateTrustDomain` / `deleteTrustDomain` legs are implemented so far; the
      remaining Trust Domain Device CRUD resource is a later slice.
    - `createTrustDomain` admits only the access types this provider advertises
      (Wi-Fi WPA-Personal/Enterprise, Thread STRUCTURED). A canonical but
      unadvertised `Thread:TLV` access detail → `400 INVALID_ARGUMENT`.
    - `createTrustDomain` validates the required fields and each access detail's
      discriminated required keys, but not the nested field *values* (SSID
      pattern, hex-string lengths, Thread channel range) nor the `policies`
      object contents (accepted and echoed as-is) — a documented cut.
    - The write-only WPA `password` is stripped from the `TrustDomain` response.
    - The `trustDomainId` (and the audit `createdBy`/`modifiedBy`) are
      deterministic version-5 UUIDs derived from the request, not random —
      re-creating the same `(serviceId, name)` re-mints the same id (that is how
      the `409` duplicate is detected). The audit `createdBy`/`modifiedBy`
      identify the token subject, not a real operator account.
    - `getService` folds a malformed (non-UUID) `serviceId` into the same `404
      NOT_FOUND` as an unknown id (the catalog is regenerated per identity rather
      than stored, so the two are indistinguishable), mirroring the sibling
      `getNetworkAccessDevice`.
    - The Services catalog is a deterministic, representative set (no real billing
      backend): the returned service/site names are simulator defaults, keyed only
      to the identity. Each `serviceSite` now carries a deterministic
      `location.geographicPoint` (a WGS-84 point stable per identity/slot); the
      canonical `location.propertyAddress` (a civic address) remains omitted (it is
      optional in the schema). The point is a fixed, renderable coordinate, not a
      queryable spatial field, so it is not a scenario control plane.
    - The advertised capabilities are a fixed, representative set covering all four
      CAMARA access-type variants' families (Wi-Fi WPA-Personal, Wi-Fi
      WPA-Enterprise, Thread) and all four policy capabilities; the exact values
      are simulator defaults (schema-valid), not negotiated per operator.
  version: "wip"
  license:
    name: Apache-2.0
    url: https://www.apache.org/licenses/LICENSE-2.0.html

servers:
  - url: "{apiRoot}/network-access-domains/vwip"
    variables:
      apiRoot:
        default: http://localhost:8080
        description: API root, e.g. the CamaraSim base URL.

paths:
  /trust-domains/capabilities:
    get:
      operationId: getTrustDomainCapabilities
      summary: Get Trust Domain capabilities
      description: |
        Retrieves the set of Trust Domain configuration capabilities supported by
        the API provider — the supported access types and their variants, and the
        policy limits (max devices, per-domain bandwidth bands, egress-list
        constraints) a caller may configure.

        There is no request body and no device identifier: capabilities are fixed
        provider configuration, so this operation has no parameter-driven control
        plane and always returns the same document (docs/DESIGN.md §7).
      security:
        - openId:
            - network-access-domains:trust-domains
      parameters:
        - $ref: "#/components/parameters/XCorrelator"
      responses:
        "200":
          description: Trust Domain capabilities supported by this API provider.
          headers:
            x-correlator:
              $ref: "#/components/headers/XCorrelator"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TrustDomainCapabilities"
              examples:
                capabilities:
                  summary: The provider's fixed Trust Domain capabilities
                  value:
                    supportedAccessTypes:
                      - accessType: "Wi-Fi:WPA_PERSONAL"
                        wifiProperties:
                          ssidRequired: true
                          supportedSecurityModes:
                            - WPA2-Personal
                            - WPA3-Personal
                            - WPA2-WPA3-Personal
                          passwordConstraints:
                            minLength: 8
                            maxLength: 63
                            requireSpecialCharacters: false
                          authServerRequired: false
                      - accessType: "Wi-Fi:WPA_ENTERPRISE"
                        wifiProperties:
                          ssidRequired: true
                          supportedSecurityModes:
                            - WPA2-Enterprise
                            - WPA3-Enterprise
                          authServerRequired: true
                      - accessType: "Thread:STRUCTURED"
                        threadProperties:
                          supportedChannels:
                            min: 11
                            max: 26
                          networkKeyFormat: "32-hex-digits"
                          networkNameConstraints:
                            minLength: 1
                            maxLength: 16
                          maxDatasetLength: 254
                    supportedPolicies:
                      maxDevices:
                        minValue: 1
                        maxValue: 100
                      maxDomainDownstreamRate:
                        minValue: 1000000
                        maxValue: 1000000000
                      maxDomainUpstreamRate:
                        minValue: 1000000
                        maxValue: 1000000000
                      egressAllowedList:
                        maxEntries: 50
                        supportedDestinationTypes:
                          - IP
                          - FQDN
                          - CIDR
        "400":
          $ref: "../../shared/errors.yaml#/components/responses/InvalidArgument"
        "401":
          $ref: "../../shared/errors.yaml#/components/responses/Unauthenticated"
        "403":
          $ref: "../../shared/errors.yaml#/components/responses/PermissionDenied"
        "500":
          $ref: "../../shared/errors.yaml#/components/responses/Internal"
        "503":
          $ref: "../../shared/errors.yaml#/components/responses/Unavailable"
      x-camarasim-scenarios:
        description: >-
          Provider capabilities are fixed operator configuration with no
          device/subscriber key, so this operation has no parameter-driven control
          plane (docs/DESIGN.md §7): a scoped token always yields the same
          document.
        cases:
          - input: a token carrying the network-access-domains:trust-domains scope
            result: "200 — the fixed TrustDomainCapabilities document"
          - input: a token without the scope
            result: "403 PERMISSION_DENIED"
          - input: no / invalid token
            result: "401 UNAUTHENTICATED"

  /trust-domains:
    post:
      operationId: createTrustDomain
      summary: Create a Trust Domain
      description: |
        Creates a Trust Domain — a group of devices on the subscriber's home
        network sharing a common access policy — for one of the caller's
        services. The caller submits a `TrustDomainCreate` (a `serviceId`, a
        human-readable `name`, an `enabled` flag, and 1–4 `accessDetails`); the
        simulator mints a server-assigned `trustDomainId`, stores the Trust
        Domain in memory, and returns the full `TrustDomain` (`201`).

        Three control planes drive the outcome (docs/DESIGN.md §7):

        1. **The token subject** — a reserved error suffix on the authenticated
           identity selects the canonical CAMARA error (an account-level plane,
           checked first, mirroring `getServices`/`getService`).
        2. **Request validation** — a missing/blank/oversized `name`, a missing
           `enabled`, a missing/malformed `serviceId`, an `accessDetails` that is
           empty or longer than 4, or an entry whose `accessType` is unknown, is
           not one this provider advertises (see
           `GET /trust-domains/capabilities`), or lacks its variant's required
           keys → `400 INVALID_ARGUMENT`.
        3. **Store state** — the `trustDomainId` is derived from the
           `(serviceId, name)` pair, so creating a Trust Domain with the same
           `name` for the same `serviceId` collides → `409` (duplicate name for
           service).
      security:
        - openId:
            - network-access-domains:trust-domains
      parameters:
        - $ref: "#/components/parameters/XCorrelator"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TrustDomainCreate"
            examples:
              wifiPersonal:
                summary: A Trust Domain with one Wi-Fi WPA-Personal access detail
                value:
                  serviceId: "3fa85f64-5717-4562-b3fc-2c963f66afa6"
                  name: "Primary WiFi Network"
                  description: "Primary home WiFi Network for all devices"
                  enabled: true
                  accessDetails:
                    - accessType: "Wi-Fi:WPA_PERSONAL"
                      ssid: "my-ssid"
                      securityMode:
                        password: "my-password"
                        securityModeType: "WPA3-Personal"
      responses:
        "201":
          description: The Trust Domain was created.
          headers:
            x-correlator:
              $ref: "#/components/headers/XCorrelator"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TrustDomain"
              examples:
                created:
                  summary: The created Trust Domain (write-only password omitted)
                  value:
                    id: "b1e0f6a2-9c3d-5e4f-8a1b-2c3d4e5f6a7b"
                    serviceId: "3fa85f64-5717-4562-b3fc-2c963f66afa6"
                    name: "Primary WiFi Network"
                    description: "Primary home WiFi Network for all devices"
                    enabled: true
                    accessDetails:
                      - accessType: "Wi-Fi:WPA_PERSONAL"
                        ssid: "my-ssid"
                        securityMode:
                          securityModeType: "WPA3-Personal"
                    createdAt: "2024-01-01T09:00:00Z"
                    createdBy: "4a7c9e2d-1b3f-5a6c-9d8e-0f1a2b3c4d5e"
                    modifiedAt: "2024-01-01T09:00:00Z"
                    modifiedBy: "4a7c9e2d-1b3f-5a6c-9d8e-0f1a2b3c4d5e"
        "400":
          $ref: "../../shared/errors.yaml#/components/responses/InvalidArgument"
        "401":
          $ref: "../../shared/errors.yaml#/components/responses/Unauthenticated"
        "403":
          $ref: "../../shared/errors.yaml#/components/responses/PermissionDenied"
        "404":
          $ref: "../../shared/errors.yaml#/components/responses/NotFound"
        "409":
          $ref: "../../shared/errors.yaml#/components/responses/Conflict"
        "429":
          $ref: "../../shared/errors.yaml#/components/responses/TooManyRequests"
        "500":
          $ref: "../../shared/errors.yaml#/components/responses/Internal"
        "503":
          $ref: "../../shared/errors.yaml#/components/responses/Unavailable"
      x-camarasim-scenarios:
        description: >-
          createTrustDomain has three control planes (docs/DESIGN.md §7): the
          token subject's reserved error suffix (checked first), request
          validation, and the in-memory store (a duplicate name-for-service
          collides). The trustDomainId is derived from the (serviceId, name)
          pair, so the same name for the same service re-mints the same id and is
          rejected as a duplicate.
        cases:
          - input: "a valid TrustDomainCreate for a fresh (serviceId, name)"
            result: "201 — the created TrustDomain (write-only password stripped)"
          - input: "a second create with the same (serviceId, name)"
            result: "409 — duplicate name for the service"
          - input: "a different name for the same serviceId"
            result: "201 — a distinct Trust Domain"
          - input: "missing/blank/over-64-char name, missing enabled, or malformed serviceId"
            result: "400 INVALID_ARGUMENT"
          - input: "accessDetails empty, longer than 4, or missing a variant's required keys"
            result: "400 INVALID_ARGUMENT"
          - input: "an accessType not advertised (e.g. Thread:TLV) or unknown"
            result: "400 INVALID_ARGUMENT"
          - input: "the token subject trailing digits are …404"
            result: "404 NOT_FOUND (reserved error suffix, account-level)"
          - input: "the token subject trailing digits are …429"
            result: "429 TOO_MANY_REQUESTS (reserved error suffix, account-level)"
          - input: a token without the network-access-domains:trust-domains scope
            result: "403 PERMISSION_DENIED"
          - input: no / invalid token
            result: "401 UNAUTHENTICATED"

  /trust-domains/{trustDomainId}:
    get:
      operationId: getTrustDomain
      summary: Retrieve a Trust Domain by id
      description: |
        Retrieves a single `TrustDomain` by the server-assigned `trustDomainId`
        that `createTrustDomain` returned.

        There is no request body and no device identifier: the operation is keyed
        only to the in-memory store (docs/DESIGN.md §7). The `trustDomainId` is an
        opaque, SHA-256-derived UUID the caller cannot construct, so it is not
        itself a scenario plane — a stored id → `200` with the persisted
        `TrustDomain` (the write-only WPA password was already stripped at
        create), any other id (never created or malformed) → `404 NOT_FOUND`. A
        malformed id folds into the same `404` (there is no store entry to
        distinguish it from an unknown one).
      security:
        - openId:
            - network-access-domains:trust-domains
      parameters:
        - $ref: "#/components/parameters/XCorrelator"
        - name: trustDomainId
          in: path
          required: true
          description: The server-assigned id of the Trust Domain to retrieve.
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: The requested Trust Domain.
          headers:
            x-correlator:
              $ref: "#/components/headers/XCorrelator"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TrustDomain"
              examples:
                trustDomain:
                  summary: A stored Trust Domain (write-only password omitted)
                  value:
                    id: "b1e0f6a2-9c3d-5e4f-8a1b-2c3d4e5f6a7b"
                    serviceId: "3fa85f64-5717-4562-b3fc-2c963f66afa6"
                    name: "Primary WiFi Network"
                    description: "Primary home WiFi Network for all devices"
                    enabled: true
                    accessDetails:
                      - accessType: "Wi-Fi:WPA_PERSONAL"
                        ssid: "my-ssid"
                        securityMode:
                          securityModeType: "WPA3-Personal"
                    createdAt: "2024-01-01T09:00:00Z"
                    createdBy: "4a7c9e2d-1b3f-5a6c-9d8e-0f1a2b3c4d5e"
                    modifiedAt: "2024-01-01T09:00:00Z"
                    modifiedBy: "4a7c9e2d-1b3f-5a6c-9d8e-0f1a2b3c4d5e"
        "400":
          $ref: "../../shared/errors.yaml#/components/responses/InvalidArgument"
        "401":
          $ref: "../../shared/errors.yaml#/components/responses/Unauthenticated"
        "403":
          $ref: "../../shared/errors.yaml#/components/responses/PermissionDenied"
        "404":
          $ref: "../../shared/errors.yaml#/components/responses/NotFound"
        "429":
          $ref: "../../shared/errors.yaml#/components/responses/TooManyRequests"
        "500":
          $ref: "../../shared/errors.yaml#/components/responses/Internal"
        "503":
          $ref: "../../shared/errors.yaml#/components/responses/Unavailable"
      x-camarasim-scenarios:
        description: >-
          getTrustDomain is keyed only to the in-memory store (docs/DESIGN.md §7).
          The trustDomainId is an opaque server-minted UUID, so store state is the
          only control plane: a stored id returns the persisted TrustDomain, any
          other id — unknown or malformed — is a 404 (folded, no store entry to
          tell them apart).
        cases:
          - input: "a trustDomainId returned by a prior createTrustDomain"
            result: "200 — the persisted TrustDomain (write-only password omitted)"
          - input: "a well-formed trustDomainId that was never created"
            result: "404 NOT_FOUND"
          - input: "a malformed (non-UUID) trustDomainId"
            result: "404 NOT_FOUND (folded, no store to distinguish it)"
          - input: a token without the network-access-domains:trust-domains scope
            result: "403 PERMISSION_DENIED"
          - input: no / invalid token
            result: "401 UNAUTHENTICATED"
    patch:
      operationId: updateTrustDomain
      summary: Update a Trust Domain by id
      description: |
        Updates a single `TrustDomain` in place by the server-assigned
        `trustDomainId` that `createTrustDomain` returned. The caller sends a
        `TrustDomainUpdate` — the mutable subset of a Trust Domain, **every field
        optional** — and only the fields present in the body are changed; an absent
        field is left unchanged. Per the CAMARA schema `accessDetails` is a **full
        replacement** (a supplied array replaces the stored one wholesale, it is not
        merged). The read-only identity/audit fields (`id`, `serviceId`,
        `createdAt`, `createdBy`) are immutable — a body that carries them has them
        ignored — and every successful update refreshes `modifiedAt`/`modifiedBy`.

        Two control planes (docs/DESIGN.md §7), mirroring the repo's other PATCH
        legs: the **request body** is validated first (so a body `400` wins over a
        `404`), then **store state** — the opaque server-minted `trustDomainId` is
        not itself a scenario plane, so a stored id → `200` with the updated
        `TrustDomain`, any other id (never created, already deleted, or malformed) →
        `404 NOT_FOUND` (folded). Unlike `createTrustDomain` there is **no `409`**:
        the operation addresses an existing resource by its fixed id and never
        re-derives it. The write-only WPA `password` is stripped from any
        replacement `accessDetails` so it never appears in the echoed `TrustDomain`.
      security:
        - openId:
            - network-access-domains:trust-domains
      parameters:
        - $ref: "#/components/parameters/XCorrelator"
        - name: trustDomainId
          in: path
          required: true
          description: The server-assigned id of the Trust Domain to update.
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TrustDomainUpdate"
            examples:
              renameAndDisable:
                summary: Rename and disable a Trust Domain
                value:
                  name: "Guest WiFi Network"
                  enabled: false
              clearDescription:
                summary: Clear the optional description (explicit null)
                value:
                  description: null
              replaceAccessDetails:
                summary: Replace the access details wholesale
                value:
                  accessDetails:
                    - accessType: "Wi-Fi:WPA_PERSONAL"
                      ssid: "new-ssid"
                      securityMode:
                        password: "new-s3cr3t"
                        securityModeType: "WPA3-Personal"
      responses:
        "200":
          description: The updated Trust Domain.
          headers:
            x-correlator:
              $ref: "#/components/headers/XCorrelator"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TrustDomain"
              examples:
                trustDomain:
                  summary: The updated Trust Domain (write-only password omitted)
                  value:
                    id: "b1e0f6a2-9c3d-5e4f-8a1b-2c3d4e5f6a7b"
                    serviceId: "3fa85f64-5717-4562-b3fc-2c963f66afa6"
                    name: "Guest WiFi Network"
                    enabled: false
                    accessDetails:
                      - accessType: "Wi-Fi:WPA_PERSONAL"
                        ssid: "my-ssid"
                        securityMode:
                          securityModeType: "WPA3-Personal"
                    createdAt: "2024-01-01T09:00:00Z"
                    createdBy: "4a7c9e2d-1b3f-5a6c-9d8e-0f1a2b3c4d5e"
                    modifiedAt: "2024-06-01T12:00:00Z"
                    modifiedBy: "4a7c9e2d-1b3f-5a6c-9d8e-0f1a2b3c4d5e"
        "400":
          $ref: "../../shared/errors.yaml#/components/responses/InvalidArgument"
        "401":
          $ref: "../../shared/errors.yaml#/components/responses/Unauthenticated"
        "403":
          $ref: "../../shared/errors.yaml#/components/responses/PermissionDenied"
        "404":
          $ref: "../../shared/errors.yaml#/components/responses/NotFound"
        "429":
          $ref: "../../shared/errors.yaml#/components/responses/TooManyRequests"
        "500":
          $ref: "../../shared/errors.yaml#/components/responses/Internal"
        "503":
          $ref: "../../shared/errors.yaml#/components/responses/Unavailable"
      x-camarasim-scenarios:
        description: >-
          updateTrustDomain has two control planes (docs/DESIGN.md §7): the request
          body (validated first, so a body 400 wins over a 404) and store state (the
          opaque server-minted trustDomainId is not a scenario plane). Every field
          in TrustDomainUpdate is optional; a present field replaces, an explicit
          null on a clearable optional field (description/expiration/policies)
          removes it, and accessDetails replaces wholesale. There is no 409 — an
          existing resource is addressed by its fixed id, so a rename cannot collide.
        cases:
          - input: "a stored trustDomainId + { name, enabled } to change"
            result: "200 — the updated TrustDomain (modifiedAt/By re-stamped)"
          - input: "a stored trustDomainId + { description: null }"
            result: "200 — the optional description is cleared"
          - input: "a stored trustDomainId + { accessDetails: [...] }"
            result: "200 — accessDetails replaced wholesale (password stripped)"
          - input: "a stored trustDomainId + an empty {} body"
            result: "200 — a no-op that only re-stamps modifiedAt/modifiedBy"
          - input: "a stored trustDomainId + a body with a serviceId/id field"
            result: "200 — read-only identity fields ignored (immutable)"
          - input: "any id + an ill-typed field (e.g. enabled: \"yes\")"
            result: "400 INVALID_ARGUMENT (body validated before the store lookup)"
          - input: "a well-formed trustDomainId that was never created"
            result: "404 NOT_FOUND"
          - input: "a malformed (non-UUID) trustDomainId"
            result: "404 NOT_FOUND (folded, no store to distinguish it)"
          - input: a token without the network-access-domains:trust-domains scope
            result: "403 PERMISSION_DENIED"
          - input: no / invalid token
            result: "401 UNAUTHENTICATED"
    delete:
      operationId: deleteTrustDomain
      summary: Delete a Trust Domain by id
      description: |
        Deletes a single `TrustDomain` by the server-assigned `trustDomainId`
        that `createTrustDomain` returned, and (conceptually) removes its
        configuration from every network access device where it is applied.

        There is no request body and no device identifier: the operation is keyed
        only to the in-memory store (docs/DESIGN.md §7). The `trustDomainId` is an
        opaque, SHA-256-derived UUID the caller cannot construct, so it is not
        itself a scenario plane — a stored id is evicted → `204 No Content`
        (single-use: a second delete of the same id finds nothing), any other id
        (never created, already deleted, or malformed) → `404 NOT_FOUND`. A
        malformed id folds into the same `404` (there is no store entry to
        distinguish it from an unknown one). Deletion is synchronous.
      security:
        - openId:
            - network-access-domains:trust-domains
      parameters:
        - $ref: "#/components/parameters/XCorrelator"
        - name: trustDomainId
          in: path
          required: true
          description: The server-assigned id of the Trust Domain to delete.
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: The Trust Domain was deleted successfully. No content.
          headers:
            x-correlator:
              $ref: "#/components/headers/XCorrelator"
        "400":
          $ref: "../../shared/errors.yaml#/components/responses/InvalidArgument"
        "401":
          $ref: "../../shared/errors.yaml#/components/responses/Unauthenticated"
        "403":
          $ref: "../../shared/errors.yaml#/components/responses/PermissionDenied"
        "404":
          $ref: "../../shared/errors.yaml#/components/responses/NotFound"
        "429":
          $ref: "../../shared/errors.yaml#/components/responses/TooManyRequests"
        "500":
          $ref: "../../shared/errors.yaml#/components/responses/Internal"
        "503":
          $ref: "../../shared/errors.yaml#/components/responses/Unavailable"
      x-camarasim-scenarios:
        description: >-
          deleteTrustDomain is keyed only to the in-memory store (docs/DESIGN.md
          §7). The trustDomainId is an opaque server-minted UUID, so store state
          is the only control plane: a stored id is evicted (204, single-use), any
          other id — unknown, already-deleted, or malformed — is a 404 (folded, no
          store entry to tell them apart).
        cases:
          - input: "a trustDomainId returned by a prior createTrustDomain"
            result: "204 No Content — the Trust Domain is evicted"
          - input: "the same trustDomainId a second time (already deleted)"
            result: "404 NOT_FOUND (single-use)"
          - input: "a well-formed trustDomainId that was never created"
            result: "404 NOT_FOUND"
          - input: "a malformed (non-UUID) trustDomainId"
            result: "404 NOT_FOUND (folded, no store to distinguish it)"
          - input: a token without the network-access-domains:trust-domains scope
            result: "403 PERMISSION_DENIED"
          - input: no / invalid token
            result: "401 UNAUTHENTICATED"

  /services:
    get:
      operationId: getServices
      summary: Retrieve the services for the authenticated identity
      description: |
        Retrieves the caller's `ServiceList` — the logical commercial
        subscriptions / account relationships (each tied to a `serviceSite`) the
        authenticated identity holds.

        There is no request body: the operation is keyed to the authenticated
        identity (the token subject), which is its control plane
        (docs/DESIGN.md §7). A list never 404s — an identity with nothing
        associated yields an empty array.
      security:
        - openId:
            - network-access-domains:services:read
      parameters:
        - $ref: "#/components/parameters/XCorrelator"
      responses:
        "200":
          description: The services associated with the authenticated identity.
          headers:
            x-correlator:
              $ref: "#/components/headers/XCorrelator"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceList"
              examples:
                twoServices:
                  summary: An identity with two services
                  value:
                    - id: "3fa85f64-5717-4562-b3fc-2c963f66afa6"
                      name: "Main Home Internet"
                      description: "Internet subscription for primary residence"
                      serviceSite:
                        id: "123e4567-e89b-12d3-a456-426614174000"
                        name: "Primary Residence"
                        description: "123 Main Street"
                        location:
                          geographicPoint:
                            latitude: 50.735851
                            longitude: 7.10066
                    - id: "123e4567-e89b-12d3-a456-426614174001"
                      name: "Vacation Home Internet"
                      description: "Internet subscription for vacation home"
                      serviceSite:
                        id: "123e4567-e89b-12d3-a456-426614174002"
                        name: "Vacation Home"
                        description: "45 Lakeside Road"
                        location:
                          geographicPoint:
                            latitude: 47.36667
                            longitude: 8.55
                empty:
                  summary: An identity with no associated services
                  value: []
        "400":
          $ref: "../../shared/errors.yaml#/components/responses/InvalidArgument"
        "401":
          $ref: "../../shared/errors.yaml#/components/responses/Unauthenticated"
        "403":
          $ref: "../../shared/errors.yaml#/components/responses/PermissionDenied"
        "404":
          $ref: "../../shared/errors.yaml#/components/responses/NotFound"
        "429":
          $ref: "../../shared/errors.yaml#/components/responses/TooManyRequests"
        "500":
          $ref: "../../shared/errors.yaml#/components/responses/Internal"
        "503":
          $ref: "../../shared/errors.yaml#/components/responses/Unavailable"
      x-camarasim-scenarios:
        description: >-
          getServices is keyed to the authenticated identity (the token subject),
          which is its control plane (docs/DESIGN.md §7). A reserved error suffix
          on the subject selects the canonical CAMARA error; otherwise the
          subject's trailing three digits `d` fix the ServiceList
          deterministically.
        cases:
          - input: "subject trailing digits …000 (or no digits)"
            result: "200 — an empty ServiceList (nothing associated)"
          - input: "subject trailing digits …001"
            result: "200 — 1 service"
          - input: "subject trailing digits …005"
            result: "200 — 2 services ((5-1) % 3 + 1)"
          - input: "subject trailing digits …003"
            result: "200 — 3 services"
          - input: "subject trailing digits …404"
            result: "404 NOT_FOUND (reserved error suffix)"
          - input: "subject trailing digits …429"
            result: "429 TOO_MANY_REQUESTS (reserved error suffix)"
          - input: a token without the network-access-domains:services:read scope
            result: "403 PERMISSION_DENIED"
          - input: no / invalid token
            result: "401 UNAUTHENTICATED"

  /services/{serviceId}:
    get:
      operationId: getService
      summary: Retrieve a single service by id
      description: |
        Retrieves a single `Service` from the authenticated identity's catalog by
        its `serviceId`.

        There is no request body: the operation is keyed to the authenticated
        identity (the token subject) and the `serviceId` path parameter
        (docs/DESIGN.md §7). The subject's reserved error suffix selects the
        canonical CAMARA error (an account-level plane, checked first); otherwise
        the `serviceId` is matched against the identity's catalog — a match →
        `200` with that `Service`, any other id (unknown, another identity's, or
        malformed) → `404 NOT_FOUND`.
      security:
        - openId:
            - network-access-domains:services:read
      parameters:
        - $ref: "#/components/parameters/XCorrelator"
        - name: serviceId
          in: path
          required: true
          description: The id of the service to retrieve.
          schema:
            $ref: "#/components/schemas/ServiceId"
      responses:
        "200":
          description: The requested service.
          headers:
            x-correlator:
              $ref: "#/components/headers/XCorrelator"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Service"
              examples:
                service:
                  summary: A single service
                  value:
                    id: "3fa85f64-5717-4562-b3fc-2c963f66afa6"
                    name: "Main Home Internet"
                    description: "Internet subscription for primary residence"
                    serviceSite:
                      id: "123e4567-e89b-12d3-a456-426614174000"
                      name: "Primary Residence"
                      description: "123 Main Street"
                      location:
                        geographicPoint:
                          latitude: 50.735851
                          longitude: 7.10066
        "400":
          $ref: "../../shared/errors.yaml#/components/responses/InvalidArgument"
        "401":
          $ref: "../../shared/errors.yaml#/components/responses/Unauthenticated"
        "403":
          $ref: "../../shared/errors.yaml#/components/responses/PermissionDenied"
        "404":
          $ref: "../../shared/errors.yaml#/components/responses/NotFound"
        "429":
          $ref: "../../shared/errors.yaml#/components/responses/TooManyRequests"
        "500":
          $ref: "../../shared/errors.yaml#/components/responses/Internal"
        "503":
          $ref: "../../shared/errors.yaml#/components/responses/Unavailable"
      x-camarasim-scenarios:
        description: >-
          getService is keyed to the authenticated identity (the token subject)
          and the serviceId path parameter (docs/DESIGN.md §7). The subject's
          reserved error suffix selects the canonical CAMARA error (checked
          first); otherwise the serviceId is matched against the identity's
          deterministic catalog. A malformed (non-UUID) serviceId folds into the
          same 404 as an unknown id (the catalog is regenerated, not stored, so
          the two are indistinguishable).
        cases:
          - input: "a serviceId drawn from the identity's catalog"
            result: "200 — that Service"
          - input: "a well-formed serviceId the identity does not hold"
            result: "404 NOT_FOUND"
          - input: "a serviceId belonging to a different identity's catalog"
            result: "404 NOT_FOUND"
          - input: "a malformed (non-UUID) serviceId"
            result: "404 NOT_FOUND (folded, no store to distinguish it)"
          - input: "any serviceId when the subject trailing digits are …000"
            result: "404 NOT_FOUND (empty catalog)"
          - input: "any serviceId when the subject trailing digits are …404"
            result: "404 NOT_FOUND (reserved error suffix, account-level)"
          - input: "any serviceId when the subject trailing digits are …429"
            result: "429 TOO_MANY_REQUESTS (reserved error suffix, account-level)"
          - input: a token without the network-access-domains:services:read scope
            result: "403 PERMISSION_DENIED"
          - input: no / invalid token
            result: "401 UNAUTHENTICATED"

components:
  securitySchemes:
    openId:
      $ref: "../../auth/openapi.yaml#/components/securitySchemes/camaraOAuth"

  parameters:
    XCorrelator:
      name: x-correlator
      in: header
      required: false
      description: Correlation id for the request, echoed back on the response.
      schema:
        type: string

  headers:
    XCorrelator:
      description: The `x-correlator` from the request, echoed if one was sent.
      required: false
      schema:
        type: string

  schemas:
    TrustDomainCapabilities:
      type: object
      additionalProperties: false
      description: |
        Describes the Trust Domain configuration capabilities supported by this
        API provider.
      required:
        - supportedAccessTypes
      properties:
        supportedAccessTypes:
          type: array
          minItems: 1
          maxItems: 4
          description: List of supported access types and their variants.
          items:
            $ref: "#/components/schemas/SupportedAccessType"
        supportedPolicies:
          type: object
          additionalProperties: false
          description: Map of supported policy types to their capability details.
          properties:
            maxDevices:
              $ref: "#/components/schemas/MaxDevicesPolicyCapability"
            maxDomainDownstreamRate:
              $ref: "#/components/schemas/BandwidthPolicyCapability"
            maxDomainUpstreamRate:
              $ref: "#/components/schemas/BandwidthPolicyCapability"
            egressAllowedList:
              $ref: "#/components/schemas/EgressPolicyCapability"

    SupportedAccessType:
      type: object
      description: |
        A supported access type. `accessType` is the discriminator; the remaining
        properties belong to the matching variant (Wi-Fi variants carry
        `wifiProperties`, Thread variants carry `threadProperties`).
      required:
        - accessType
      discriminator:
        propertyName: accessType
      properties:
        accessType:
          type: string
          enum:
            - "Wi-Fi:WPA_PERSONAL"
            - "Wi-Fi:WPA_ENTERPRISE"
            - "Thread:STRUCTURED"
            - "Thread:TLV"
          description: The access type identifier.
        wifiProperties:
          $ref: "#/components/schemas/WiFiAccessTypeProperties"
        threadProperties:
          $ref: "#/components/schemas/ThreadAccessTypeProperties"

    WiFiAccessTypeProperties:
      type: object
      additionalProperties: false
      description: The configuration properties supported for a Wi-Fi access type.
      properties:
        ssidRequired:
          type: boolean
          example: true
        supportedSecurityModes:
          type: array
          maxItems: 5
          items:
            type: string
            enum:
              - WPA2-Personal
              - WPA3-Personal
              - WPA2-WPA3-Personal
              - WPA2-Enterprise
              - WPA3-Enterprise
        passwordConstraints:
          $ref: "#/components/schemas/PasswordConstraints"
        authServerRequired:
          type: boolean
          example: false

    PasswordConstraints:
      type: object
      additionalProperties: false
      description: The constraints on a Wi-Fi pre-shared-key / password.
      required:
        - minLength
        - maxLength
      properties:
        minLength:
          type: integer
          format: int32
          minimum: 8
          maximum: 63
          example: 8
        maxLength:
          type: integer
          format: int32
          minimum: 8
          maximum: 63
          example: 63
        requireSpecialCharacters:
          type: boolean
          example: false

    ThreadAccessTypeProperties:
      type: object
      additionalProperties: false
      description: The configuration properties supported for a Thread access type.
      properties:
        supportedChannels:
          type: object
          additionalProperties: false
          required:
            - min
            - max
          properties:
            min:
              type: integer
              minimum: 11
              maximum: 26
              example: 11
            max:
              type: integer
              minimum: 11
              maximum: 26
              example: 26
        networkKeyFormat:
          type: string
          enum:
            - "32-hex-digits"
        networkNameConstraints:
          type: object
          additionalProperties: false
          required:
            - minLength
            - maxLength
          properties:
            minLength:
              type: integer
              minimum: 1
              maximum: 16
              example: 1
            maxLength:
              type: integer
              minimum: 1
              maximum: 16
              example: 16
        maxDatasetLength:
          type: integer
          minimum: 1
          maximum: 254
          example: 254

    MaxDevicesPolicyCapability:
      type: object
      additionalProperties: false
      description: The supported range for the max-devices-per-domain policy.
      required:
        - maxValue
        - minValue
      properties:
        minValue:
          type: integer
          format: int32
          minimum: 1
          maximum: 1024
          example: 1
        maxValue:
          type: integer
          format: int32
          minimum: 1
          maximum: 1024
          example: 100

    BandwidthPolicyCapability:
      type: object
      additionalProperties: false
      description: The supported range (bits/second) for a per-domain bandwidth policy.
      required:
        - maxValue
        - minValue
      properties:
        minValue:
          type: integer
          format: int64
          minimum: 0
          maximum: 10000000000
          example: 1000000
        maxValue:
          type: integer
          format: int64
          minimum: 0
          maximum: 10000000000
          example: 1000000000

    EgressPolicyCapability:
      type: object
      additionalProperties: false
      description: The constraints on a Trust Domain egress allow-list policy.
      required:
        - maxEntries
        - supportedDestinationTypes
      properties:
        maxEntries:
          type: integer
          format: int32
          minimum: 1
          maximum: 1024
          example: 50
        supportedDestinationTypes:
          type: array
          maxItems: 3
          items:
            type: string
            enum:
              - IP
              - FQDN
              - CIDR

    ServiceId:
      type: string
      format: uuid
      description: |
        A unique identifier that represents a service account. A valid UUID (RFC
        4122, versions 1-5) using only lowercase hexadecimal characters.
      example: "3fa85f64-5717-4562-b3fc-2c963f66afa6"

    ServiceList:
      type: array
      description: A list of services accessible to the calling identity.
      maxItems: 100
      items:
        $ref: "#/components/schemas/Service"

    Service:
      type: object
      description: |
        A service represents a logical commercial subscription or account
        relationship between a network operator and a subscriber (residential,
        commercial, public Wi-Fi, or other service types).
      required:
        - id
      properties:
        id:
          $ref: "#/components/schemas/ServiceId"
        name:
          type: string
          maxLength: 64
          description: Optional friendly name for the service.
          example: "Main Home Internet"
        description:
          type: string
          maxLength: 255
          description: Optional description of the service.
          example: "Internet subscription for primary residence"
        serviceSite:
          $ref: "#/components/schemas/ServiceSite"

    ServiceSite:
      type: object
      description: |
        A Service Site represents a subscriber location associated with one or
        more Network Access Devices.
      required:
        - id
      properties:
        id:
          $ref: "#/components/schemas/ServiceId"
        name:
          type: string
          maxLength: 64
          description: The name of the service site.
          example: "Primary Residence"
        description:
          type: string
          maxLength: 64
          description: A textual description of the service site.
          example: "123 Main Street"
        location:
          $ref: "#/components/schemas/Location"

    Location:
      type: object
      description: |
        The physical location of a service site. The canonical schema carries a
        WGS-84 `geographicPoint` and a civic `propertyAddress`; CamaraSim emits a
        deterministic `geographicPoint` only (`propertyAddress` is a documented
        cut). The point is a fixed, representative coordinate the caller can
        render, not a queryable spatial field, so it is not a scenario control
        plane (docs/DESIGN.md §7).
      properties:
        geographicPoint:
          $ref: "#/components/schemas/Point"

    Point:
      type: object
      description: A WGS-84 point (decimal degrees).
      required:
        - latitude
        - longitude
      properties:
        latitude:
          type: number
          format: double
          minimum: -90
          maximum: 90
          example: 50.735851
        longitude:
          type: number
          format: double
          minimum: -180
          maximum: 180
          example: 7.10066

    # --- Trust Domain create (createTrustDomain) --------------------------

    Uuid:
      type: string
      format: uuid
      minLength: 36
      maxLength: 36
      pattern: "^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$"
      description: |
        A UUID (RFC 4122, versions 1-5) using only lowercase hexadecimal
        characters. CamaraSim mints server-assigned ids (the `trustDomainId`
        and the audit `createdBy`/`modifiedBy`) as deterministic, name-based
        version-5 UUIDs.
      example: "b1e0f6a2-9c3d-5e4f-8a1b-2c3d4e5f6a7b"

    DateTime:
      type: string
      format: date-time
      maxLength: 64
      description: |
        An RFC 3339 date-time with a time zone. CamaraSim emits the audit
        `createdAt`/`modifiedAt` stamps as UTC instants with a `Z` offset.
      example: "2024-01-01T09:00:00Z"

    Policies:
      type: object
      additionalProperties: true
      description: |
        Trust Domain policy limits (max devices, per-domain up/downstream
        bandwidth, egress allow-list) a caller may configure. CamaraSim accepts
        and echoes the object but does not validate its contents against the
        advertised capabilities — the full `Policies` shape is a documented cut.
        See `GET /trust-domains/capabilities` for the supported policy ranges.

    WpaPersonalDetail:
      type: object
      additionalProperties: false
      description: WPA Personal security mode configuration for Wi-Fi networks.
      required:
        - password
      properties:
        password:
          type: string
          writeOnly: true
          minLength: 8
          maxLength: 255
          pattern: "^[\\x20-\\x7E]{8,63}$"
          description: |
            The pre-shared key for the WPA Personal Wi-Fi network. Write-only —
            it is never echoed in a `TrustDomain` response.
          example: "my-password"
        securityModeType:
          type: string
          enum:
            - WPA2-Personal
            - WPA2-WPA3-Personal
            - WPA3-Personal
          description: |
            The security mode type for the WPA Personal Wi-Fi network. Leave
            blank to auto-select.
          example: "WPA3-Personal"

    WpaEnterpriseDetail:
      type: object
      additionalProperties: false
      description: WPA Enterprise security mode configuration for Wi-Fi networks.
      required:
        - securityModeType
      properties:
        authServer:
          type: string
          maxLength: 255
          description: The authentication server for the WPA Enterprise Wi-Fi network.
          example: "1.2.3.4"
        securityModeType:
          type: string
          enum:
            - WPA2-Enterprise
            - WPA3-Enterprise
          description: The security mode type for the WPA Enterprise Wi-Fi network.
          example: "WPA3-Enterprise"

    WiFiWpaPersonalAccessDetail:
      type: object
      additionalProperties: false
      description: Access details for Wi-Fi networks using WPA Personal security mode.
      required:
        - accessType
        - securityMode
      properties:
        accessType:
          type: string
          enum:
            - "Wi-Fi:WPA_PERSONAL"
          example: "Wi-Fi:WPA_PERSONAL"
        ssid:
          type: string
          minLength: 2
          maxLength: 32
          pattern: "^(?! )[\\x20-\\x7E]{2,32}(?<! )$"
          description: SSID (2-32 printable ASCII characters, no leading/trailing space).
          example: "my-ssid"
        securityMode:
          $ref: "#/components/schemas/WpaPersonalDetail"

    WiFiWpaEnterpriseAccessDetail:
      type: object
      additionalProperties: false
      description: Access details for Wi-Fi networks using WPA Enterprise security mode.
      required:
        - accessType
        - securityMode
      properties:
        accessType:
          type: string
          enum:
            - "Wi-Fi:WPA_ENTERPRISE"
          example: "Wi-Fi:WPA_ENTERPRISE"
        ssid:
          type: string
          minLength: 2
          maxLength: 32
          pattern: "^(?! )[\\x20-\\x7E]{2,32}(?<! )$"
          description: SSID (2-32 printable ASCII characters, no leading/trailing space).
          example: "my-ssid"
        securityMode:
          $ref: "#/components/schemas/WpaEnterpriseDetail"

    ThreadStructuredAccessDetail:
      type: object
      additionalProperties: false
      description: Access details for Thread networks using the structured format.
      required:
        - accessType
        - channel
        - extendedPanId
        - networkKey
        - networkName
        - panId
      properties:
        accessType:
          type: string
          enum:
            - "Thread:STRUCTURED"
          example: "Thread:STRUCTURED"
        channel:
          type: integer
          format: int32
          minimum: 11
          maximum: 26
          description: The Thread channel (IEEE 802.15.4 channel number).
          example: 13
        extendedPanId:
          type: string
          pattern: "^[0-9a-fA-F]{16}$"
          maxLength: 16
          description: The Extended PAN ID (16 hex digits).
          example: "d63e8e3e495ebbc3"
        networkKey:
          type: string
          pattern: "^[0-9a-fA-F]{32}$"
          maxLength: 32
          description: The Thread Network Key (32 hex digits).
          example: "dfd34f0f05cad978ec4e32b0413038ff"
        networkName:
          type: string
          minLength: 1
          maxLength: 16
          description: The Thread Network Name (1-16 ASCII characters).
          example: "Spec-Thread-B3AF"
        panId:
          type: string
          pattern: "^[0-9a-fA-F]{4}$"
          maxLength: 4
          description: The PAN ID (4 hex digits).
          example: "d63e"

    ThreadTlvAccessDetail:
      type: object
      additionalProperties: false
      description: |
        Access details for Thread networks using the TLV-encoded operational
        dataset. Present in the canonical schema, but **not advertised** by this
        provider (see `GET /trust-domains/capabilities`), so `createTrustDomain`
        rejects a Thread:TLV access detail with `400 INVALID_ARGUMENT`.
      required:
        - accessType
        - operationalDataset
      properties:
        accessType:
          type: string
          enum:
            - "Thread:TLV"
          example: "Thread:TLV"
        operationalDataset:
          type: string
          maxLength: 255
          description: The Thread operational dataset encoded as a TLV hex string.
          example: "0e08000000000000010010000102030405060708090a0b0c0d0e0f"

    AccessDetail:
      oneOf:
        - $ref: "#/components/schemas/WiFiWpaPersonalAccessDetail"
        - $ref: "#/components/schemas/WiFiWpaEnterpriseAccessDetail"
        - $ref: "#/components/schemas/ThreadStructuredAccessDetail"
        - $ref: "#/components/schemas/ThreadTlvAccessDetail"
      discriminator:
        propertyName: accessType
        mapping:
          "Wi-Fi:WPA_PERSONAL": "#/components/schemas/WiFiWpaPersonalAccessDetail"
          "Wi-Fi:WPA_ENTERPRISE": "#/components/schemas/WiFiWpaEnterpriseAccessDetail"
          "Thread:STRUCTURED": "#/components/schemas/ThreadStructuredAccessDetail"
          "Thread:TLV": "#/components/schemas/ThreadTlvAccessDetail"
      required:
        - accessType
      description: |
        Network access details for one access type. `accessType` is the
        discriminator; the remaining properties belong to the matching variant.

    TrustDomainUpdate:
      type: object
      description: The updatable fields of a Trust Domain.
      properties:
        name:
          type: string
          maxLength: 64
          description: Human-readable name for the Trust Domain.
          example: "Primary WiFi Network"
        description:
          type: string
          maxLength: 255
          description: Detailed description of the Trust Domain.
          example: "Primary home WiFi Network for all devices"
        enabled:
          type: boolean
          description: Whether the Trust Domain is currently enabled.
          example: true
        expiration:
          allOf:
            - $ref: "#/components/schemas/DateTime"
          description: |
            The expiration date and time of the Trust Domain. If omitted, the
            Trust Domain does not automatically expire.
        policies:
          $ref: "#/components/schemas/Policies"
        accessDetails:
          type: array
          minItems: 1
          maxItems: 4
          description: |
            The access configurations that define how devices may join the Trust
            Domain (1-4 entries). On creation the full list must be supplied.
          items:
            $ref: "#/components/schemas/AccessDetail"

    TrustDomainCreate:
      allOf:
        - $ref: "#/components/schemas/TrustDomainUpdate"
        - type: object
          properties:
            serviceId:
              $ref: "#/components/schemas/ServiceId"
      required:
        - name
        - enabled
        - accessDetails
        - serviceId
      description: The request body used to create a Trust Domain.

    TrustDomain:
      allOf:
        - type: object
          required:
            - id
          properties:
            id:
              allOf:
                - $ref: "#/components/schemas/Uuid"
              readOnly: true
              description: The server-assigned Trust Domain identifier.
        - $ref: "#/components/schemas/TrustDomainCreate"
        - type: object
          properties:
            createdAt:
              allOf:
                - $ref: "#/components/schemas/DateTime"
              readOnly: true
              description: When the Trust Domain was created.
            createdBy:
              allOf:
                - $ref: "#/components/schemas/Uuid"
              readOnly: true
              description: The identity that created the Trust Domain.
            modifiedAt:
              allOf:
                - $ref: "#/components/schemas/DateTime"
              readOnly: true
              description: When the Trust Domain was last modified.
            modifiedBy:
              allOf:
                - $ref: "#/components/schemas/Uuid"
              readOnly: true
              description: The identity that last modified the Trust Domain.
      description: |
        A Trust Domain. On the `createTrustDomain` response the write-only WPA
        `password` inside any `accessDetails` entry is omitted.
